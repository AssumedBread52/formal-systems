import { Box } from '@/common/components/box/box';
import { ReactElement } from 'react';

export const EditProfilePage = (props: any): ReactElement => {
  console.log(props);

  return (
    <Box>
      Edit Profile Page!
    </Box>
  );
};
