import { Metadata } from 'next';
import { ReactElement } from 'react';

export const StatementPage = (): ReactElement => {
  return (
    <h1>
      Statement Page
    </h1>
  );
};

export const generateMetadata = (): Metadata => {
  return {
  };
};
